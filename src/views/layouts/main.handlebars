<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TaskApp</title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <style>
        /* Loader - Spinner element shown during page load*/
        .loader {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border: 8px solid #f3f3f3;
            border-top: 8px solid #444;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 1s linear infinite;
            z-index: 1000;
        }
        /* Loader Animation - Defines the spinning animation for the loader */
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        /* after loading, hide the loader */
        .loaded .loader {
            display: none;
        }
        /* prevent scrolling while the content is loading */
        .no-scroll {
            overflow: hidden;
        }
    </style>
</head>
    <!-- while loader is active, scrolling is disabled -->
    <body class="no-scroll"> 
    <!-- Loader -->
    <div class="loader" id="loader"></div>
    <!-- Navbar-->
    {{>_navbar}}
    {{!----}}
    <div class="container mt-4"> 
        {{!-- Passport Error - Display error messages from Passport --}}
        {{#if error}}
            <div class="alert alert-danger">{{error}}</div>
        {{else}}
        {{/if}}
        {{!-- Flash Messages - Display success or error messages --}}
        {{>_msg}} 
        {{!-- body - Main content of the page --}}
        {{{body}}}
    </div>
    <!-- Bootstrap and dependency scripts -->
    <script 
        src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" 
        integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" 
        crossorigin="anonymous">
    </script>
    <script 
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" 
        crossorigin="anonymous">
    </script>
    <script src="/js/bootstrap.js"></script>
    {{!-- shortcut: file:///c:/nodejs/taskApp/public/js/main/main.js --}}
    <script type="module" src="/js/main/main.js"></script>
</body>
</html>
